# Base URL of the Lunedoc API service.
# Local dev expects services/api/ running on :8000 (no Docker —
# Postgres + Redis are host services; see services/api/README.md).
VITE_API_BASE_URL=http://localhost:8000/api/v1
